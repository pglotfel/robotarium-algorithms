function [IPs, V] = lyap_gtg( handles, pose)
%LYAP_GTG A control lyapunov function for go-to-goal behavior.
%   handles: incoming data from Robotarium.  pose: desired pose of robot


    if(handles.count > 1) %Only run if new data is available.      
        
        data = handles.data{handles.count - 1}; %Grab current data.
        
        IPs = data(1,:); 
        r_poses = data(2:3, :); 
        thetas = data(4,:);
        
        for i = 1:
        
    end

end

